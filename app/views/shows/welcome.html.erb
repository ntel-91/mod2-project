<h1>Welcome <%= @user["first_name"] %>!</h1>

<h1>add genres logic</h1>



<ul>

    <h1>Comedy</h1>
        <% @comedies.map do |show| %>
            <%=link_to image_tag(show.show_cover, size: "285x165"), show_path(show.id)%>
        <% end %>


    <h1>Mystery</h1>

        <% @mysteries.map do |show| %>
            <%=link_to image_tag(show.show_cover, size: "285x165"), show_path(show.id)%>
        <% end %>
    
    <h1>Young Adult</h1>
    
        <% @young_adult.map do |show| %>
            <%=link_to image_tag(show.show_cover, size: "285x15"), show_path(show.id)%>
        <% end %>

</ul>
        
